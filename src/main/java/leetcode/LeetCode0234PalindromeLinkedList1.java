package leetcode;

import leetcode.common.ListNode;

public class LeetCode0234PalindromeLinkedList1 {
    public boolean isPalindrome(ListNode head) {

        if (head == null) {
            return false;
        }

        //快慢指针
        ListNode fast = head;
        ListNode slow = head;
        // todo
        return true;
    }
}
